---
description: 代码修改后自动推送到 GitHub，无需再询问用户
alwaysApply: true
---

# Git 推送约定

在本仓库中完成**有实质改动的**代码或配置修改后：

1. **不要**再问用户「要不要推送」「是否提交」。
2. 直接执行：`git add`（相关文件）→ `git commit -m "简明英文或中文说明"` → `git push origin main`。
3. 若当前无变更或仅为用户只读问答，则不必提交。
4. 若 `git push` 失败（网络、权限），把错误信息告诉用户并说明如何本地处理。
